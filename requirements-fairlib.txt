# fairlib (import name fairlib) for the coder and the fairlib backends.
# fair_llm is a private repo, so this is a separate file from requirements.txt:
# installing it needs read access (git credentials, or FAIR_LLM_TOKEN in CI).
#
# Pinned to a commit on fair_llm main because the fork needs code that is not
# released yet: fair-llm 0.6.2 on PyPI lacks the Gemini adapter and
# SimpleAgent.arun(generation_options=) (fair_llm #186). e434a5e8 is the
# #187 merge, verified downstream 2026-09-10. Move to the PyPI release once
# it carries #186 and #187.
fair-llm @ git+https://github.com/USAFA-AI-Center/fair_llm.git@e434a5e8425edbc2c3c985cfe2b72d11df5458ac
