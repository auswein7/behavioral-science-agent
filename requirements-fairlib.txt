# fairlib (import name fairlib) for the coder and the fairlib backends.
# fair_llm is a private repo, so this is a separate file from requirements.txt:
# installing it needs read access (git credentials, or FAIR_LLM_TOKEN in CI).
#
# Pinned to the v0.6.3 release tag's commit: the first release carrying the
# Gemini adapter and SimpleAgent.arun(generation_options=) (fair_llm #186),
# the planner prompt fix (#187) and prompt-builder validation (#191).
# fair-llm 0.6.2 on PyPI lacks all three. The commit, not the tag name, so
# the pin cannot move under us; verified downstream 2026-09-10. Switch to
# fair-llm==0.6.3 once 0.6.3 is published to PyPI.
fair-llm @ git+https://github.com/USAFA-AI-Center/fair_llm.git@69e2025fd813841a95b4b49f1967ab840cb5466b
